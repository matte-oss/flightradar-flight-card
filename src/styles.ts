import { css } from 'lit';

export const cardStyles = css`
  p {
    margin: 0;
  }

  a {
    text-decoration: none;
    color: inherit;
  }

  @keyframes pulse {
    50% {
      opacity: 0.5;
    }
  }
`;
